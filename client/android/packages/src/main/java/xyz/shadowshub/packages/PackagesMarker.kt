package xyz.shadowshub.packages

/**
 * packages：包体系（app/pet-layer/api/skill/theme/toolpkg/mcp/workflow/model-pack/url-app）。
 * 统一流水线：校验 → 不可变版本 → 安装 → 权限求交 → 审计 → 回滚。
 * M2-1（包流水线）时填充；M0-1 仅建模块。
 */
object PackagesMarker